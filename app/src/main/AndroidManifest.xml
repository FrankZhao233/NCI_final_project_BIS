<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.me.flea">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".FleaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.WelfareDetailActivity"></activity>
        <activity android:name=".ui.GoodsAuctionActivity" />
        <activity android:name=".ui.MyGoodsListActivity" />
        <activity
            android:name=".ui.GoodsDetailActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.ResetPasswordActivity"
            android:label="" />
        <activity android:name=".ui.PublishActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MainAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SignUpActivity"
            android:label="" />
        <activity
            android:name=".ui.LoginActivity"
            android:label="" />
        <activity android:name=".ui.ChatActivity" />
        <activity android:name=".ui.ImageCropActivity" />

        <service android:name=".service.MessageService" />
    </application>

</manifest>